server:
  port: 9999
spring:
  application:
    name: @artifactId@
  cloud:
    nacos:
      username: @nacos.username@
      password: @nacos.password@
      discovery:
        server-addr: ${NACOS_HOST:127.0.0.1}:${NACOS_PORT:8848}
        watch:
          enabled: true
        watch-delay: 1000
      config:
        server-addr: ${spring.cloud.nacos.discovery.server-addr}

    gateway:
      routes:
        - id: spring-cloud-oauth-resource
          uri: lb://spring-cloud-oauth-resource
          predicates:
            - Path=/res/**
        - id: spring-cloud-oauth-authorization
          uri: lb://spring-cloud-oauth-authorization
          predicates:
            - Path=/auth/**
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true
  config:
    import:
      - optional:nacos:application-@profiles.active@.yml

knife4j:
  gateway:
    enabled: true
    strategy: discover
    discover:
      version: openapi3
      enabled: true
      excluded-services:
        - @artifactId@
        - ${spring.cloud.gateway.routes[1].id}
      service-config:
        spring-cloud-oauth-resource:
          group-name: 资源服务
          order: 1
    tags-sorter: order
    operations-sorter: order
